= simple_form_for([:admin,tracker], :wrapper => :bootstrap, :html => { :class => 'form-horizontal' }) do |f|

  %fieldset
    .form-inputs
      = f.input :name, :input_html => { :class => "span3" } 
      = f.input :description, :input_html => { :class => "span3", :rows => 7 }

    .form-actions
      = f.button :submit
