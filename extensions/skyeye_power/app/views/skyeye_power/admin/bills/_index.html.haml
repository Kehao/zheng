%table.table.table-striped.table-bordered.table-condensed
  %tr
    %th 金额(元)
    %th 用量 
    %th 是否已缴费
    %th 记录时间
    %th 上次抄表见数
    %th 本次抄表见数
    %th 操作
  - company_account.bills.each do |bill|
    %tr{:id=>dom_id(bill)}
      %td= bill.cost  
      %td= bill.amount  
      %td= bill.paid  
      %td{:width=>100}= bill.record_time.strftime("%Y-%m-%d") if  bill.record_time 
      %td= bill.last_number  
      %td= bill.this_number  
      %td
        = link_to '修改', [:edit,:admin,company_account,bill],:remote=>true 
        = link_to '删除', [:admin,company_account,bill],:remote=>true,:method=>:delete
= modal_footer do 
  =link_to "添加账单",[:new,:admin,company_account,:bill],:remote=>true,:class=>"btn"
