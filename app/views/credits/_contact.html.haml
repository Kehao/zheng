- if company.credit.present?
  - cr = company.credit
  %table.table.table-striped.table-bordered
    %tr
      %th{:colspan=>2}
        联络信息
    - contact_attrs.each do |item|
      %tr
        %td.span2
          = Credit.human_attribute_name(item) 
        %td.span4
          = cr.send(item)
