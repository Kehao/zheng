%li.alert.alert-success
  - if a.owner == current_user
    = link_to "我",""
  - else
    = link_to a.owner.try(:name),""
  新添加了客户
  = link_to a.recipient.name,""
  = a.created_at.strftime("%y-%m-%d")
